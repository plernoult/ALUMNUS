<div class= "profile">
<p><%=  link_to "⬅︎", chatrooms_path%></p>
<div class="avatar"> <%=cl_image_tag @receiver.photo.key%></div>&nbsp;&nbsp;&nbsp;
<% if %>
<% current_user.first_name == @chatroom.receiver_name %>
  <h1><%= @chatroom.sender_name %></h1>
<% else %>
  <h1><%= @chatroom.receiver_name %></h1>
<% end %>
</div>
<div id="messages" data-chatroom-id="<%= @chatroom.id %>" class="container bordure">
  <% @chatroom.messages.each do |message| %>
    <% if message.user == current_user%>
      <div class="speech-bubble">
      <%= render'messages/message', message: message %>
      </div>
    <% else %>
      <div class="speech-bubble">
      <%= render'messages/message', message: message %>
      </div>
    <% end %>
  <% end %>
</div>
<%= simple_form_for [ @chatroom, @message ], remote: true do |f| %>
  <%= f.input :content, label: false, input_html: { autocomplete: "off" }, placeholder: "Type a message @#{@chatroom.receiver_name}" %>
<% end %>
